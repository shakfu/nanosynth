# Compiler

SCgf binary serialization. Most users reach this through `SynthDef.compile()`;
`compile_synthdefs()` is the entry point for writing several definitions into a
single bundle. The byte layout is specified in [SCgf Format](../scgf-format.md).

::: nanosynth.compiler
